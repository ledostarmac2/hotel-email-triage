# Architecture

The current architecture source of truth is `docs/ARCHITECTURE.md`.

This root file is kept as a pointer because earlier architecture notes described a Next.js-first direction that is no longer the active runnable app. Future agents should read the multi-agent handoff docs before changing the project:

1. `AGENTS.md`
2. `docs/ARCHITECTURE.md`
3. `docs/CURRENT_STATE.md`
4. `docs/HANDOFF.md`
